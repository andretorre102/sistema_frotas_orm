# Preencha com seus dados locais do banco de dados PostgreSQL
DB_USER=seu usuário
DB_PASSWORD=sua senha
DB_HOST=seu host (ex: localhost)
DB_PORT=sua porta (ex: 5432)
DB_NAME=nome do seu banco de dados